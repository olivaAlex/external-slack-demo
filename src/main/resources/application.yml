spring:
  application:
    name: 'slack.demo'
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui

server:
  port: 8080
  address: 0.0.0.0
  servlet:
    context-path: /api
